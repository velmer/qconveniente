apiVersion: v1
kind: Service
metadata:
  name: estabelecimento-service
  labels:
    name: estabelecimento-service
spec:
  ports:
    - port: 8081
      targetPort: 8081
  clusterIP: None
  selector:
    role: estabelecimento-service